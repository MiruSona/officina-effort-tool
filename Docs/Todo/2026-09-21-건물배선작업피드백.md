# EffortTool 사용 피드백 — 건물 2채 배선 (2026-09-21)

게임 저장소(ProjectMozzi2)에서 서브에이전트 한 판(코드 배선 → Unity 확인 → 문서·기억)을 돌리며 쓴 기록이다.

## 잘 된 것

- `scan --all` 뒤 `estimate "1. 코드 배선:구현:M" "2. Unity 확인:실측:S" "3. lint·문서·mem:문서:S"` 가 한 번에 돌았다.
  소단계 이름에 숫자·점·가운뎃점(`·`)이 들어가도 그대로 먹었다.
- 근거 칸에 `실측 n=109` 처럼 표본 수가 찍혀서, 나온 숫자를 얼마나 믿을지 바로 알 수 있었다.

## 헷갈린 것

- 예상이 「1분」 「1분 미만」 으로 나와 **사람 시간 55분짜리 일과 나란히 놓기 어려웠다.**
  README 와 스킬에 「본줄만 센다 · 예상 칸에 그대로 옮겨 적지 말라」가 적혀 있어 오해는 안 했지만,
  보고의 공수 표에는 결국 사람 시간을 따로 적고 실측값은 괄호로 붙였다.
  **`estimate` 표 아래에 「이 값은 본줄 시간이다」 한 줄이 같이 찍히면** 표를 그대로 붙일 때 안전하겠다.
- 서브에이전트 한 판 안에서는 `scan` 이 아직 그 판의 기록을 못 읽는다 (세션 JSONL 이 끝나야 들어온다).
  그래서 **이번 판의 「실제」 칸은 툴로 못 채웠다.** 서브에이전트가 스스로 자기 공수를 재는 길이 없다.
  지금은 메인 세션이 판이 끝난 뒤 `actual` 로 채워야 한다.

## 이번 판의 눈금 (다음 예상에 쓸 것)

- 「그림 1종을 오브젝트에 잇기」(상수 2줄 + Setup 시그니처 넓히기 + Manager 호출부 + Unity Play 확인 + 문서 5군데)
  = 서브에이전트 한 판. 사람 시간 눈금 약 50~60분.
