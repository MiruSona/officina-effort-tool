# 떡 아이콘 시안 작업 피드백 (2026-09-21)

게임 저장소에서 서브에이전트가 떡 아이콘 시안 두 벌(PIL 그림 14장 + 비교 그림)을 그리며 `effort` 2.1.278 을 쓴 기록이다.
앞 문서(`2026-09-20-서브에이전트구간못잼피드백.md` · `2026-09-21-픽셀그림작업피드백.md`)에 이어 쓴다. 거기 적힌 것은 가리키기만 한다.

## 잘 된 것 (지난번에서 나아진 것)

- **서브에이전트 Skill 목록에 `effort-usage` 가 떴다.** 지난 문서 「막힌 것 1」(서브에이전트에서 스킬이 안 뜬다)은
  이번 판에서는 안 나왔다 — `Tools/EffortTool/README.md` 를 읽은 뒤 목록에 바로 붙었다.
- `estimate 구현:M 실측:S` 가 표를 바로 내줬다. 근거 칸의 `실측 n=109` 로 표본 수가 보여 값을 믿을지 판단할 수 있었다.

## 막힌 것

| # | 무엇 | 어떻게 됐나 |
| --- | --- | --- |
| 1 | **돌고 있는 갈래는 스스로를 못 잰다** | 내 갈래가 끝나기 전에는 내 transcript 가 안 닫혀서 `scan` 뒤에도 내 작업이 캐시에 없다. `show` 로 보이는 것은 **부모 세션의 앞 작업과 그 앞 갈래**뿐이었다. 그래서 「실제」 칸을 내 것으로 채울 수 없고, 부모 세션 벽시계로 에둘러 적어야 했다. 앞 문서 「막힌 것 2」(start/stop 이 없다)와 같은 뿌리로 보인다 |

## 바라는 것

- 앞 문서의 `effort mark start/stop` 바람에 한 표 더한다. 서브에이전트가 **자기 판의 시작·끝을 스스로 찍을 수 있으면**
  갈래가 끝나기 전에도 「실제」 칸을 실측으로 적을 수 있다. 지금은 갈래마다 손 측정으로 돌아간다.
