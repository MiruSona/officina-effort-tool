# EffortTool 진행상황

> 2026-08-31 첫 판. 여기부터 읽는다.

## 이미 정한 것

| 무엇 | 정한 것 | 근거 |
| --- | --- | --- |
| 언어·배포 | Go 단일 exe `effort.exe`, 바깥 라이브러리 0개, `CGO_ENABLED=0` | 설계 1절 · mem 20260831-496580c4 |
| 명령 | `scan` `stats` `estimate` `show` `list` `rules` `group` `actual` (+ `version` `help`) | 설계 2절 · 2차설계 6-1 |
| 작업 단위 | 작업 한 건 = `promptId` 한 개. 서브에이전트 줄은 같은 `promptId` 로 붙는다 | 설계 0절 |
| 중복 제거 | `(requestId, message.id)` last-wins | mem 20260831-4dcdb496 |
| 파생 저장소 | `%USERPROFILE%\.effort\` — `cache/` 는 지워도 되는 파생물, `rules.txt` 만 정본 | 설계 3-3 |
| 예상 셈법 | 분류별 p50 × 크기 배율, 표본 5건 미만이면 시드, 12건 이상이면 실측 | 설계 5절 |
| 출력 | 한국어 파이프 표 (기본) · `--wide` 는 직접 폭 계산 | 설계 6-1 |
| 분류 칸 | 일 여섯 + 미분류 + **일 아닌 셋(대화·도구실행·잡무)**. 일 아닌 칸은 예상 표본에서 뺀다 | 분류개선설계 1절 · mem 20260831-98e693a7 |
| 분류 차례 | 아홉 순위. 알림은 **같은 세션 앞 일 칸 분류를 물려받는다**(사슬 금지) | 분류개선설계 2절 |
| 규칙 경계 | 순서·칸 이름은 코드에, 낱말·도구 집합·접두·상한은 `rules.txt` 에 | 분류개선설계 3-3 |
| **벽시계 뜻** | **본줄 구간만.** 곁줄(큐·파일이력·자리비움)은 시간을 못 늘린다. 본줄은 코드에 박은 **넣는 목록**. 서브에이전트 구간(`agent_wall_ms`)은 참고값으로 보여주기만 한다 | 2026-09-04 뒤집음 · mem 20260904-de657849 |
| **소단계 정본** | `~/.effort/groups.txt` — `rules.txt` 와 나란한 두 번째 정본. 캐시가 아니라 `--rebuild` 로 안 날아간다 | 2차설계 결정 4 |
| **`estimate` 표본 단위** | **묶음(소단계)** 이 기본, `--unit task` 로 옛 셈. 표시가 없으면 세션 안 30분 간격 자동 묶기 | 2차설계 결정 5·6 |
| 캐시 판 | **4** (판이 다르면 저절로 전면 재스캔). 3 → 4 는 벽시계를 본줄만으로 되돌린 것 | 2026-09-04 |
| 크기 배율 | **S 0.35 · M 1.0 · L 2.35 · XL 6.49** — 2026-09-04 묶음 단위 실측(일 칸 묶음 252건, 본줄만). `effort rules --measure` 로 다시 잰다 | 2차설계 10-3 · mem 20260904-de657849 |

## 지금 상태

> 2026-09-01 **2차 개선까지 반영**.

- 첫 판 1~12 · 분류 개선 1~10 · **2차 개선 1~14** 다 끝났다. 시험 179개 통과(2차 · 09-01) → **3차(09-04) 뒤 193개 통과**(2026-09-06 `go test ./...` 로 다시 셈), `gofmt`·`go vet` 깨끗.
- 실측 (2차 · 09-01 시점) : 세션 111개 · 작업 1192건 · 줄 27806개 (캐시 판 3, 전면 재스캔). 3차(09-04) 시점 값은 아래 「작업 1400건」 줄에 있다.
- **벽시계를 합집합으로 바꿨다.** 총계 83.2h → **117.7h**. 설계 기대(25~33h)를 벗어난 까닭은
  곁줄이 아니라 **서브 구간 합집합이 94.9h** 이기 때문이다 (9.3시간 살아 있던 배경 갈래가 실물로 있다).
  **본줄만은 24.8h** 로 기대와 맞는다. `MainWallMs`·`AgentWallMs` 가 캐시에 따로 있다.
- **미분류 66건(5.5%)**. 짝짓기 309 · 이어받기 90 · 앞말이어짐 17 · Monitor→도구실행 54.
- **2026-09-04 3차** : `scan` 이 `rules.txt` 지문(sha256)을 보고 알아서 전면 재분류한다 ·
  `list --group` 표본을 `estimate` 와 같게 맞췄다 (일 칸 묶음 252 · `조사` 100 = `estimate n=100`) ·
  이어짐을 **사슬**로 바꾸고 글머리 낱말 13개를 더했다 (**앞말이어짐 25 → 36 · 미분류 76 → 65**,
  기본값 rules.txt 로 잰 값. 새로 잡은 13건 중 눈으로 본 오탐 3건).
- **2026-09-04 벽시계를 본줄만으로 되돌렸다** (캐시 판 3 → 4). 작업 1400건 총계 **164.6h → 29.0h**
  (서브 구간 138.0h 는 `agent_wall_ms` 에 그대로 남아 `show`·`list --group` 에 「서브(참고)」로만 나온다).
  `순수시간잘림` 은 118건 그대로다. 묶음 수 252 그대로, 대표 분류만 조금 움직였다
  (구현 48 → 51 · 실측 21 → 19 · 검토 13 → 11).
- **배율·시드를 본줄 기준으로 다시 잡았다** : `S/M/L/XL` = **0.35 / 1.0 / 2.35 / 6.49**
  (일 칸 묶음 252건 · p20 0.7 · p50 1.9 · p80 4.5 · p95 12.4분). 시드는 표본 12건을 넘는 다섯 분류만 갈았다
  (검토 11건 · 미분류 0건은 옛 값 그대로). `effort rules --measure` 로 언제든 다시 낸다.
- **`estimate` 값이 사람 시간과 자릿수가 다시 벌어졌다** — `구현:L` 이 17분 → **4분**이다.
  본줄만 세니 당연한 결과다. 사람 시간으로 옮기려면 `human` 배율(회차 15~20건)이 있어야 한다.
- 명령 둘이 늘었다 : `group`(소단계 경계 표시) · `actual`(예상↔실제 배율).
- `scan` 이 **모르는 줄 종류**를 건수와 함께 찍는다. 2026-09-21 에 Claude Code 새 판의 넷
  (`artifact-autoreact-ledger` · `artifact-comment-monitor` · `frame-link` · `custom-title`)을
  「알고 뺀 곁줄」에 넣어, 지금은 실물에서 모르는 종류가 안 나온다.

## 2026-09-21 사용 피드백 반영

게임 저장소 실사용 피드백 7장(`Docs/Todo/` 의 「피드백」·「안쓴까닭」)을 한 묶음으로 고쳤다.
각 문서 맨 위에 `> 반영 (2026-09-21)` 한 줄이 붙어 있다.

| 무엇 | 어떻게 닫았나 |
| --- | --- |
| `estimate` 옵션 차례 표기가 거꾸로 | README·`help` 를 `[옵션…] [소단계…]` 로. `help estimate` 에 「옵션은 소단계 앞에」 |
| 서브에이전트 판을 누가 재나 | README 새 절 「서브에이전트가 한 일 재기」 · SKILL 새 절. **판이 끝난 뒤 메인이 잰다** |
| 서브모듈 저장소의 명령 앞자리 | README·SKILL 의 표를 **세 벌**로 (`.\Tools\EffortTool\bin\effort.exe`) |
| `estimate` 값을 사람 시간으로 오해 | 표 아래 푸터 한 줄 「이 값은 본줄 시간이다 …」 (골든 갱신) |
| 시각이 UTC 로 읽힌다 | `list` 머리 `날짜(+09:00)` · `show` 에 `-07:00`. 시험 `zone_test.go` |
| 캐시에 없는 묶음 경고가 안 읽힌다 | `printMissingMarks` 문구를 두 줄로 — 왜 빠졌나 · 무엇을 하면 되나 |
| 모르는 줄 종류 넷 | `knownSideTypes` 에 등록. 시험 `TestArtifactLinesAreKnownSide` |
| `--human` 인데 칸이 `—` | 빈 줄이 있으면 푸터로 왜 비었는지 알린다. 시험 둘 |
| 제목 깨짐 · 바이트 자르기 | 코드가 아니라 원본·터미널 문제라 README 「알아 둘 것」에 두 줄 |
| 붙인 저장소에 안내가 없다 | README 새 절 「다른 저장소에 붙일 때」 — 훅 글에 넣을 한 줄. **훅 설치 기능은 안 만든다** |
| 옛 exe 가 남아 옛 버그를 갖는다 | `build.ps1` 새로 만듦. `--version` 이 `커밋 · 빌드시각` 을 찍는다 (안 박혔으면 `dev`) |

**안 만든 것** (피드백에 있었지만 이번에 뺀 것) : `effort mark start/stop` · `show --live` ·
서브 구간을 총계에 넣는 옵션 · 「대기」 분류 · 사람 확인이 끼는 일의 배율. 남은 것은 아래 「다음에 할 일」에 있다.

## 2026-09-23 사용 피드백 반영 3

피드백 여섯 장(09-21~22) 가운데 네 장에서 되풀이된 `estimate` 인자 오류문을 고쳤다.

| 무엇 | 어떻게 고쳤나 |
| --- | --- |
| `검증:M` 이 「모르는 분류 "M"」 | 두 칸 다 분류가 아니면 **앞 칸**을 찍는다. 뒤 칸이 크기면 「크기(M)는 맞습니다. 분류 자리가 틀렸습니다」 (`ArgError`) |
| 쓸 수 있는 분류를 몰라 `rules.txt` 를 연다 | 오류문 끝에 분류 목록(규칙 `word` 표에서 읽은 일 칸) + 보기 `조사:M` · `이름:조사:M`. 규칙을 못 읽으면 기본 규칙으로 안내 (`ExplainArgError` · `rulesForHelp`) |
| 제목 통째를 넣으면 안내 없이 막힌다 | 같은 안내가 붙는다. 시험 넷 (`parse_test.go`) |

`estimate` 가 실제와 스무 배 어긋나는 까닭은 조사만 했다 → `Docs/Research/2026-09-23-estimate표본단위조사.md`
(뿌리 : 표본이 메인 본줄 묶음인데 일은 서브 구간에서 돈다 · 추천 : 근거 칸에 표본 단위·중앙값 찍기).

추천 (a) 를 같은 날 넣었다 : 근거 칸 `실측 n=111 · 본줄 묶음 중앙 1.8분 (서브 포함 3.9분)` · `--json` 에 `Unit`·`SampleN`·`SampleP50Ms`·`WithAgentP50Ms` ·
표 밑 「이 표본은 메인 세션 본줄 묶음이다 …」 한 줄 · 묶음에 `with_agent_ms`(본줄∪서브) · `render.Minutes` 반올림(1.8분 → 2분, 골든 갱신).
표본 값 자체와 예상 셈법은 그대로다.

리뷰 6건 수정 : `리뷰스킬:검증` 은 뒤 칸을 찍는다(뒤 칸이 크기일 때만 앞 칸) · 서브 포함 중앙이 본줄 중앙과 같으면 괄호 뺌 ·
`--metric pure` 꼬리말 · 「칸은 반올림이라 합과 ±1분 어긋날 수 있다」 · Minutes 시험 두 줄 · word 표가 비면 분류 목록을 전체 일 칸으로.

**estimate 표본에 서브 구간을 넣었다** (사용자 결정, 같은 날). 새 기본 `--metric total` = 본줄 ∪ 서브,
서브 몫은 표본 하나당 `rules.txt` 새 줄 `sub max 120`(분) 으로 자른다 — 빠지면 `scan` 이 덧붙인다(새 종류 `sub`).
옛 동작은 `--metric wall`. 근거 칸 `실측 n=113 · 서브 포함 묶음 중앙 4.1분 (본줄 1.8분)`, 꼬리말에 상한과 걸린 수,
JSON 은 `MainlineP50Ms` · `WithAgentP50Ms` · `Capped` (`SampleP50Ms` 는 없앰). `show`·`stats`·`list` 총계는 그대로 본줄만(09-04 결정 유지).
실물 : `조사:M 구현:L 실측:S` 가 7분 → 24분 (표본 19건이 상한에 걸림). 시드·크기 배율은 본줄 기준 그대로라 다시 재야 한다 — 아래 「다음에 할 일」.

리뷰 5건 수정 · actual 도 total : 예상·실제가 같은 잣대가 되게 `actual` 기본도 `total`(실제 칸 = 본줄 ∪ 서브, 같은 `sub max` 로 자름, 꼬리말 「예상·실제 모두 <metric> 기준」) · total 꼬리말을 「서브 몫은 묶음 하나당 최대 N분」 + 「서브 구간 안의 대기(승인 등)는 상한까지 들어간다」로 · 본줄 0 표본은 본줄 중앙에서 뺌 · 같은 분류 두 줄·상한 경계 시험.

## 2026-09-21 코드 리뷰 수정

사용 피드백 반영 판을 리뷰해 나온 8건을 고쳤다.

| 무엇 | 어떻게 고쳤나 |
| --- | --- |
| 하위 명령 `-h`/`--help` 가 종료 1 + 「옵션이 잘못됐습니다」 | `parseFlags` 가 `flag.ErrHelp` 를 갈라 그 명령의 도움말을 stdout 에 찍고 종료 0. flag 의 영어 사용법은 `fs.Usage` 를 비워 막았다. 시험 `TestSubcommandHelpExitsZero` (여덟 명령 × 두 꼴) |
| `stats --by group --json` 앞에 한국어 경고가 샌다 | `printMissingMarks` 를 stderr 로. 부르는 네 곳이 한 번에 낫는다. 시험 `TestGroupStatsJSONStaysClean` |
| `list` 머리 시간대가 서머타임에서 한 시간 어긋난다 | `listDateColumn` 이 찍을 줄들에서 오프셋을 뽑는다. 줄마다 갈리면 머리는 `날짜`, 줄에 오프셋을 붙인다. 시험 `TestListDateColumnUsesRowOffsets` (LA · 1월·7월) |
| `.git` 없는 자리에서 `build.ps1` 이 죽는다 | 네이티브 git 호출을 `try/catch` 로 감싸고 `2>$null` 을 뺐다 (PS 5.1 + `Stop` 조합이 예외로 터진다) |
| 박은 커밋이 미커밋 변경을 못 알린다 | `git status --porcelain -- cmd internal go.mod` 가 비지 않으면 `-dirty`. 박는 커밋은 그대로 「그 경로를 마지막으로 건드린 커밋」이고, README 문장을 그 뜻으로 고쳤다 |
| `--from` 표의 사람눈금 `0` | `ParseTable` 이 0·음수를 사용법 오류로 막는다 (「없으면 칸을 비웁니다」). 시험 `TestParseTableRejectsNonPositiveHuman` |
| 「`mark` · `--actual` 은 아직 없다」가 헷갈린다 | README·SKILL 을 「시간을 손으로 적어 넣는 길은 없다」로 풀어 썼다 |

## 다음에 할 일

| 할 일 | 왜 | 크기 |
| --- | --- | --- |
| `total` 기준으로 크기 배율·시드 다시 재기 | 09-23 기본이 본줄 ∪ 서브로 바뀌었는데 `size`·`seed` 는 본줄 표본(09-04)에서 잰 값이다. `rules --measure` 도 본줄만 본다 | S |
| 사람 눈금 배율(`human`) 다시 잡기 | `actual` 로 **회차 15~20건**이 쌓인 뒤에 고친다. 지금은 벽시계가 본줄만이라 예상 표를 사람 시간으로 옮길 길이 이것뿐이다 | S |
| 사용자 `~/.effort/rules.txt` 의 `contfirst` 에 새 낱말 더하기 | 기본값 덧붙이기는 **빠진 종류**만 채우고 있는 종류의 낱말은 안 건드린다. 새 낱말 13개는 손으로 넣어야 실물 캐시에 든다 | S |
| 검토 분류 시드 다시 재기 | 묶음 표본이 11건이라 12건 컷에 걸려 옛 짐작값(10/5/25)이 그대로다 | S |
| 사람 확인이 끼는 일(그림·다듬기) 눈금 | 09-21 피드백 : `조사:S 구현:M 검토:M` 합 4분 ↔ 실제 27분. 분류를 더할지 배율을 따로 둘지 아직 안 정했다 | M |
| 외부 대기가 큰 일의 눈금 | 그림 생성 8분 같은 구간이 본줄에 안 잡혀 「1분 미만」에 몰린다. 「대기」 분류는 이번에 안 만들었다 | M |
| 서브에이전트가 자기 판을 재는 길 | 지금은 **불가능**하고 메인이 뒤에 잰다. `mark`·`--live` 는 이번에 안 만들었다 | L |

## 못 정한 것

- (끝남) **배경 갈래를 벽시계에 넣을지** — 2026-09-04 **안 넣는다**로 정해졌다 (mem `20260904-de657849`).
- **짝짓기 309·앞말이어짐 17이 기대(380~395 · 28~35)보다 낮은 까닭.** 알림 본문에 `<task-id>` 가
  있어도 그 갈래 파일이 캐시에 없으면 옛 이어받기로 떨어진다 — 어느 쪽이 몇 건인지 아직 안 갈랐다.
- 큰 세션에서 JSONL 합이 `cost-state` 보다 작은 **정확한 까닭** (지금 실물 76%). 배분하지 않고 나란히 보여주는 것으로 피해 갔다.
- `groups.txt` 는 **소급 적용이 안 된다** — 지난 기록의 묶음은 전부 자동이라 `actual` 은 오늘 이후부터 쓸모가 있다.

> 닫힌 미결 : compaction(8건 전부 manual, 안 깨짐) · `spawnDepth ≥ 2`(depth 2 = 34건 · 3 = 3건, `show` 에 표시) ·
> 알림 짝짓기(열쇠는 본문 `<task-id>`) · 여러 기계 합치지 않기(mem `20260901-9a66e5bf`).

## 2026-09-18 코드 리뷰 후속

전체 툴 코드 리뷰 판에서 이 툴은 발견 10건 중 7건(E1~E7)을 고쳤다 (11 패키지 ok · `bin/effort.exe` 다시 구움).
아래가 남은 것이다.

### 보류 등급 (동작을 안 바꾸는 정리)

| # | 자리 | 무엇 · 왜 남았나 |
| --- | --- | --- |
| E8 | `internal/store/scanstate.go:66` | `NeedsFullRead` · `Offset` · `Reader.Offset` 이 죽은 코드다. 읽는 사람이 증분 스캔을 잘못 이해하게 만든다 |
| E9 | `internal/paths/jail.go:33` | `Jail.Resolve`/`OpenRead` 가 실제 읽기 경로에 안 쓰인다 → **서브에이전트 파일에 심링크 검사가 없다** |
| E10 | `cmd/effort/common.go:78` | 물려받음 규칙 이름이 세 벌 · `shortID` 가 두 벌 |

### 이번 판에서 새로 드러난 것

- **`rules.txt` 에 `min`·`blend` 줄이 아예 없으면 여전히 0/0 이다.** E4 는 「값이 이상할 때」만
  막았다. 줄 자체가 없는 경우는 `MissingKinds` 에 넣어 알려야 한다.
- **`group --add` 에 빈 이름을 주면 종료 4(쓰기 오류)로 나간다.** 사용법 잘못이니 종료 1 이 맞다.
- **`Slug` 가 UTF-16 두 칸을 먹는 글자(이모지)에서 Claude Code 와 다를 수 있다.** E5 로 영숫자 규칙은
  맞췄지만 서러게이트 쌍은 안 재봤다.
- 저장소가 CRLF 라 `gofmt -l` 이 전 파일을 찍는다 (전부터 그랬다).
