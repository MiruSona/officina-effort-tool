# 익명이 손님 그림 조사에서 쓴 느낌 (2026-09-22)

## 잘 된 것

- `README.md` 를 한 번 읽으니 `effort-usage` 스킬이 목록에 떠서, 서브에이전트에서도 쓸 수 있었다.
- `estimate 조사:L 문서:L` 이 실측 표본(n=204 · n=125)으로 바로 표를 내줬다.

## 걸린 것

- **크기 눈금이 조사·문서 쪽에서 너무 짧다.** `조사:M 문서:M` 이 합 3분, `조사:L 문서:L` 이 합 7분으로 나왔다.
  이번 판은 웹 검색 네 번 + 그림 12장 확인 + 300줄 문서 쓰기였는데 L 로도 7분이다.
  `XL` 을 써야 맞는 것인지, 아니면 조사·문서 표본이 「짧은 확인 작업」에 쏠려 있는 것인지 모르겠다.
  **표에 표본의 중앙값이 어느 정도 일감인지 한 줄 보이면** 크기를 고르기 쉬울 것 같다.
- README 대로 **서브에이전트는 자기 판을 못 쟀다.** 예상만 적고 실제는 「메인 세션이 잰다」로 넘겼다.
  이건 알려진 것이라 막힌 것은 아니고, 보고 때마다 같은 설명을 쓰게 된다는 것만 적어 둔다.
