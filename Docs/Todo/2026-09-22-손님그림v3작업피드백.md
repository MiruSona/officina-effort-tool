# 손님 그림 v3 작업에서 본 것 (2026-09-22)

- `estimate` 에 자연어 제목을 그대로 넣으면 **「모르는 분류」** 로만 막힌다 (`estimate "손님 그림 v3 시안 …"`).
  거절 글에 **쓸 수 있는 분류 목록**(조사·설계·구현·실측·검토 …)을 같이 찍어 주면 `rules.txt` 를 열어 보지 않아도 된다.
- 크기 글자(S·M·L)를 분류 자리에 쓰면 `모르는 분류 "M"` 이 나온다. 이때도 `<분류>:<크기>` 꼴을 한 줄 보여 주면 빠르다.
